--- c
---  o

DROP GROUP IF EXISTS c;
DROP TABLE IF EXISTS o;
DROP TABLE IF EXISTS c;

CREATE TABLE c(
  cid INT NOT NULL PRIMARY KEY,
  x INT
);

CREATE TABLE o(
  oid INT NOT NULL PRIMARY KEY,
  cid INT,
  GROUPING FOREIGN KEY(cid) REFERENCES c(cid),
  y INT
);

--- x

DROP TABLE IF EXISTS x;

CREATE TABLE x(
  xid INT
);
